[pytest]
testpaths = tests
addopts = -n logical
addpopts = --ignore=terramatch-researcher-api
; Use below option to temporarily disable pytest-xdist. See https://yossarian.net/til/post/disabling-pytest-xdist-without-removing-it/
; addopts = -n 0

;addopts = -q --tb=line